vibe main

yeet "vibez"

slay test_if() {
    sus x normie = 1
    ready (x % 2 == 0) {
        vibez.spill("Even")
    } otherwise {
        vibez.spill("Odd")
    }
}

slay main_character() {
    test_if()
    damn
}
