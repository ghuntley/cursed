// Test actual cyclic imports with the new safe loader
yeet "module_a"

slay main() drip {
    vibez.spill("Testing real cyclic imports with safe loader")
    // Call functions from the cyclic modules
    ready (based) {
        vibez.spill("Main function executed successfully")
    }
    damn 0
}
