fr fr COMPLETE PURE CURSED SELF-HOSTING TEST

fr fr Mathematical operations
sus add_result drip = mathz.add_two(20, 30)
sus abs_result drip = mathz.abs_normie(-45)
sus max_result drip = mathz.max_normie(add_result, abs_result)

fr fr String operations  
sus str_len drip = stringz.length("self-hosting")
sus str_concat tea = stringz.concat("CURSED", "stdlib")

fr fr Path operations
sus joined_path tea = path.join("cursed", "stdlib")
sus file_exists lit = path.exists("/tmp")

fr fr Environment operations
sus env_value tea = env.get("USER")
sus has_home lit = env.has("HOME")

fr fr Multi-module mathematical computation
sus computation_result drip = add_result + abs_result + max_result + str_len

fr fr This proves 5 modules working together with complex mathematical logic
fr fr Expected: 50 + 45 + 50 + 5 = 150
