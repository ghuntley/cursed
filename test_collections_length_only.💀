vibe main
yeet "vibez"
yeet "collections"

slay main_character() {
    vibez.spill("=== Test collections.length only ===")
    
    sus test_array normie[value] = normie[value]{100, 200, 300}
    vibez.spill("Array created successfully")
    
    vibez.spill("Array length:", collections.length(test_array))
    vibez.spill("Test completed")
    damn
}
