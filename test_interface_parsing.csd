fr fr Test interface parsing

collab Drawable {
    slay draw()
    slay area() meal
    slay perimeter() meal
}

collab Container<T> {
    slay add(item T)
    slay get(index normie) T
    slay size() normie
}

squad Circle {
    spill radius meal
}

slay main() {
    vibez.spill("Interface parsing test completed")
}

main()
