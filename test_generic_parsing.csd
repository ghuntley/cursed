fr fr Test generic type parsing

slay sort<T>(arr []T) []T {
    vibez.spill("Generic function with type parameter T")
    damn arr
}

slay main() {
    sus numbers []normie = [1, 2, 3]
    sus sorted []normie = sort<normie>(numbers)
    vibez.spill("Generic function test completed")
}

main()
