slay main() normie {
    sus point normie = (10, 20, 30);
    sus (x, y, z) normie = point;
    vibez.spill("Destructured values:");
    vibez.spill(x);
    vibez.spill(y);
    vibez.spill(z);
    yolo x + y + z;
}
