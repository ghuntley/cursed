sus main() -> std_int {
    yap("Testing arithmetic parsing");
    sus a drip = 10;
    sus b drip = 5;
    sus add_result drip = a + b;
    sus sub_result drip = a - b;
    sus mul_result drip = a * b;
    sus div_result drip = a / b;
    yap("All arithmetic operations parsed successfully");
    damn 0;
}
