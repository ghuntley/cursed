vibe main

slay add(a normie, b normie) normie {
    damn a + b
}

slay subtract(x normie, y normie) normie {
    damn x - y  
}

slay multiply(m normie, n normie) normie {
    damn m * n
}

slay main_character() {
    fr fr Test Oracle Steps 2.1-2.4
    
    fr fr Step 2.2: Let statements
    sus a normie = 10
    sus b normie = 5
    sus c normie = 2
    
    fr fr Step 2.1: Function calls
    sus sum normie = add(a, b)
    sus diff normie = subtract(a, b) 
    sus product normie = multiply(sum, c)
    
    fr fr Step 2.3: Assignment statements
    a = 20
    b = 30
    c = add(a, b)
    
    fr fr Complex expressions
    sus final normie = multiply(add(a, b), subtract(c, diff))
    
    damn
}
