facts message = "Hello, CURSED World!";
facts age = 25;
facts is_cool = true;

slay calculate_age(birth_year) {
    facts current_year = 2024;
    facts age = current_year - birth_year;
}

slay greet_user(name, age) {
    facts greeting = "Hello";
    facts user_info = name;
}
