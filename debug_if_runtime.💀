vibe main

yeet "vibez"

slay test_if(x normie) {
    ready (x % 2 == 0) {
        vibez.spill("Even")
    } otherwise {
        vibez.spill("Odd")
    }
}

slay main_character() {
    test_if(1)
    damn
}
