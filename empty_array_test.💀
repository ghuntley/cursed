vibe main
yeet "vibez"

slay main_character() {
    sus arr normie[] = []
    vibez.spill("Empty array length:", len(arr))
    
    sus new_arr normie[] = append(arr, 42)
    vibez.spill("After append length:", len(new_arr))
    vibez.spill("First element:", new_arr[0])
}
