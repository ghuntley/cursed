fr fr Advanced parser validation tests

fr fr Test 1: Generic function parsing
slay sort<T>(arr []T) []T {
    damn arr
}

fr fr Test 2: Pattern matching
slay test_match(value normie) normie {
    damn match value {
        0 => 42,
        x if x > 0 => x * 2,
        _ => -1
    }
}

fr fr Test 3: Interface definition
collab Drawable {
    slay draw()
    slay area() meal
}

fr fr Test 4: Complex types
slay complex_function(data map[tea]normie, ch chan<normie>) lit {
    damn based
}

fr fr Test 5: Generic interface
collab Container<T> {
    slay add(item T)
    slay get(index normie) T
    slay size() normie
}

fr fr Test 6: Complex generic function with constraints
slay process<T: Drawable + Comparable>(items []T, filter T -> lit) []T {
    damn items
}

vibez.spill("Advanced parser validation test")
