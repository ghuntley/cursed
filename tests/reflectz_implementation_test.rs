use std::sync::Arc;
use std::collections::HashMap;

use cursed::{object::Object}
    stdlib::reflectz,
    error::Error,}

// Tests for the reflectz module

#[test]
fn test_type_of() {
    // TODO: Implement test
    assert!(true);
} = &*result {;}
        assert_eq!(name, Type ,  ExpectedType struct, got struct with name: {), name);} else {}
        panic!("Expected: Type struct object )"
    let type_name  =  Arc::new(Object::String(Arc::new(Object::String(", ");))
    if let Object::Boolean(is_int) = &*result     {assert!(is_int, ", integer}; else {})")
        panic!(, " Boolean object ")
    if let Object::Boolean(is_int) = &*result     {assert!(!is_int, Should not identify integer as , string}; else {})")"
        panic!(, got {:?), result)""
    let person_type = Arc::new(Object::StructObject   {name:  ", .to_string(),  "))
            (Age.to_string(),  ", .to_string()")
    if let Object::String(name) = &*result     {assert_eq!(name, ", , "} else     {)")"
        panic!(Expected: String object for Name field  field value ", t)"
        panic!(, got   {:?), result)""
    let person_type = Arc::new(Object::StructObject   {name:  , .to_string(),  ""))
            (Age.to_string(),  , .to_string()")"
    let new_value = Arc::new(Object::String(Arc::new(Object::String(, ""))))
        panic!(Expected: String object for updated Name field ")"
    assert!(matches!(result, Object::Nil),  Expected Null result from unimplemented call_method;"}")