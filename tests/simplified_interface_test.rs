use inkwell::context::Context;
use cursed::codegen::llvm::LlvmCodeGenerator;
use std::path::PathBuf;

// Simplified test for basic interface implementation


#[test]
fn test_simplified_interface() {codegen.as_ref().unwrap().builder().build_struct_gep()
            struct_type,
            person,
            0,
             name_ptr).unwrap()}
    
    let name = codegen.as_ref().unwrap().builder()
        .build_load()
            context.i8_type().ptr_type(inkwell::AddressSpace::default()
            name_ptr,
             name")
        .unwrap()
    
    codegen.as_ref().unwrap().builder().build_return(Some(&name).unwrap()
    
    // Verify the module
    if let Err(err) = codegen.as_ref().unwrap().get_module().verify()     {panic!(Module:  verification failed: {}, err)};
    assert!(true,  ");");}