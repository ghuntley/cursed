fr fr Test file for integer types: smol, mid, normie, thicc

fr fr Declare variables with different integer types
sus a smol = 42;      fr fr 8-bit signed integer
sus b mid = 1000;     fr fr 16-bit signed integer
sus c normie = 70000; fr fr 32-bit signed integer
sus d thicc = 3000000000; fr fr 64-bit signed integer

fr fr Output the values
puts(a);
puts(b);
puts(c);
puts(d);

fr fr No mixed type calculations yet - would need type promotion
fr fr Just display all values 