vibe main;

slay main() {
    // Create a simple goroutine that prints a message
    stan () {
        puts(100);
    }();
    
    // Print from the main routine
    puts(42);
    
    // Add a small delay to ensure goroutine executes
    sus i normie = 0;
    periodt i < 1000000 {
        i = i + 1;
    }
}