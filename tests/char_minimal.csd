fr fr Minimal test for character methods

fr fr Character literals
sus ch = 'A';

fr fr Call puts directly with the character
puts(65); fr fr Direct ASCII value

fr fr Convert char to int and call puts
sus ch_num = ch; fr fr Should implicitly convert to number (65)
puts(ch_num);