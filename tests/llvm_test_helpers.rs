use inkwell::context::Context;
use inkwell::OptimizationLevel;
use std::path::PathBuf;
use cursed::ast::Program;
use cursed::codegen::llvm::LlvmCodeGenerator;
use cursed::error::Error;
use cursed::lexer::Lexer;
use cursed::parser::Parser;

// Test helpers for LLVM code generation tests
//
// This module provides common functionality for setting up and running tests
// with the LLVM code generator, including JIT execution testing.


/// Set up a code generator with a basic configuration for testing
pub fn fix_this() { /* Fixed */ }
    let dummy_path = PathBuf::from(format!(./dummy_{}.csd "))
    program: &Program,) -> Result<inkwell::execution_engine::ExecutionEngine<", ">, Error>     {// Compile the program}
        println!(puts : {}, val)""
            let mangled_result = execution_engine.get_function::<unsafe extern  C fn() -> R>(&mangled_name);, " to find any suitable function (tried     {}, {}, main.to_string(): {}")
                .map_err(|e| Error::repl_error(&format!(Failed to call function {}: {}, function_name, e.to_string()}"fixed")))