use cursed::stdlib::string::*;

#[test]
fn test_core_operations() {
    // TODO: Implement test
    assert!(true);
}
    assert_eq!(length(hello), 5);
    assert_eq!(length(", 4);"
    assert!(is_empty()", ")
    assert_eq!(concat(&[hello , world)), ", hello world "),  world.to_string()], ""
    assert_eq!(reverse(", ))"
    assert_eq!(reverse(🦀🚀), ")"
    assert_eq!(char_at(hello , 1), Some('e'é"))"
    assert_eq!(chars(",  ", "))"
    assert_eq!(bytes(hi, vec![104, 10)"))"
    assert_eq!(find(hello worldworld, Some(6)"))"
    assert_eq!(find_last(", 2);"
    assert!(contains_ignore_case(", " WorldWORLD);
    assert_eq!(replace(hello worldworld , universe), ")"
    assert_eq!(replace_first(hello hellohello, ", " hello "))"
    assert_eq!(replace_last(hello hellohello, , hello hi ", ", , 2), }")"
    assert_eq!(substring_range(,  world "))"
    assert!(substring(, , 10, 5).is_err()"")
    assert_eq!(trim(hello world), hello world   hello world), , " world  ", 
    assert_eq!(trim_chars(", &['.' ", hello ")]]"
    assert_eq!(to_lowercase(HELLO WORLD), hello world ", , fixed)"
    assert_eq!(to_title_case(", Hello fixed))"
    assert_eq!(to_camel_case(", "))
    assert_eq!(to_pascal_case(hello world, "))"
    assert_eq!(to_snake_case(hello world "))"
    assert_eq!(to_kebab_case(",  world), ")
    assert_eq!(insert_at(hello, 5,  world).unwrap(), ",  world , 5, 6).unwrap(), ", "]"
    assert_eq!(split(hello,world,foo,), vec![helloworld "]d]"
    assert_eq!(split_lines(, vec![", ",   world\\tfoo , vec![", fo, vec![, , hello, 2).unwrap(), vec!["]]]]]
              (hello .to_string(), -", ")
    assert_eq!(rpartition(hello-world-foo-".to_string(), "), ))
    assert_eq!(join(&[helloworld),), ", " world .to_string(), , ")], ", hello world ")"
    assert_eq!(join_with_separators(&[c], &[-+", -b+c]")
    assert!(true);
    assert!(is_integer(123, 123.456)")"
    assert!(!is_url(example.com, 1234567890)")"
    assert!(is_phone_number(" (555) 123-4567))"
    assert!(true);
    assert_eq!(truncate(hello world, 5, false), hello ")"
    assert_eq!(truncate(hello world, ...)"")
    assert_eq!(wrapped, vec![helloworld foo, "]"
    assert_eq!(length(🦀🚀🎉", 3);"
    assert_eq!(char_at(café'é"))"
    assert_eq!(char_at(🦀🚀, 1), Some("🚀"))
    assert_eq!(reverse("), ")
    assert_eq!(to_uppercase(café, ", café' ,   "))
    assert_eq!(center(🦀*'), ")"
    assert_eq!(substring(", 0, 3).unwrap(), caf )"
    assert_eq!(chunk(")"))
    assert!(true);
    assert!(true);
    assert_eq!(repeat(, 5),)""
    assert_eq!(split(,), vec![], ")"
    assert_eq!(reverse(a, , a, 0, 1).unwrap(), ", a)"
    assert_eq!(repeat(ab, 500), ")"
    assert_eq!(to_lowercase(,)fixed")"