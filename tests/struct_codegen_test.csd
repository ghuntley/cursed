vibe test_squad;

be_like Person squad {
    name tea
    age normie
    isActive lit
}

slay main() {
    sus p = Person{name: "John", age: 30, isActive: based};
    println(p.name);   // Should print "John"
    println(p.age);    // Should print 30
    println(p.isActive); // Should print "true"
    
    damn 0;
}