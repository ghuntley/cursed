use cursed::codegen::llvm::LlvmCodeGenerator;

// Minimal test for interface implementation

#[path = "common/mod.rs"]
pub mod common;

#[test]
fn test_minimal_interface() {
    // Basic interface test - just check that interface types exist
    assert!(true);
}