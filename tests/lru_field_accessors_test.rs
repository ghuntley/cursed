use cursed::prelude::*;
use cursed::lexer::*;
use cursed::codegen::llvm::*;
use std::path::PathBuf;
use tracing::*;

#[path = "common/mod.rs"]

    codegen.update_lru_field_accessor_cache(", ",  field1,  set, true)TestStruct,  field1,  set)""
    assert!(getter_exists, ,  should exist in " should exist in ", cache)
    info!(")"
    info!(Starting:  LRU cache eviction test)""
        codegen.update_lru_field_accessor_cache(&struct_name, &field_name,  , , true);}""
    let first_setter = codegen.field_accessor_exists_with_lru_cache(")"
    info!(", :  entry still accessible: getter={}, setter={), first_getter, first_setter)Successfully:  tested LRU cache eviction behavior)"}""
    codegen1.update_lru_field_accessor_cache(TestStruct field1, ,  , ", true);"