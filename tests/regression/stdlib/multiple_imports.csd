// Multiple module imports
yeet "mathz"
yeet "stringz" 
yeet "arrayz"

sus numbers []drip = [1, -2, 3, -4, 5]
sus text tea = "testing"

// Use functions from different modules
sus abs_sum drip = 0
sus i drip = 0
bestie (i < len(numbers)) {
    abs_sum = abs_sum + abs_normie(numbers[i])
    i = i + 1
}

sus message tea = to_upper(text) + " COMPLETE"
vibez.spill("Absolute sum:", abs_sum)
vibez.spill("Message:", message)
