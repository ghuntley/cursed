// Expression parsing tests
sus arithmetic drip = (5 + 3) * 2 - 1
sus comparison lit = x > 5 && y < 10
sus logical lit = based || cringe
sus string_concat tea = "hello" + " " + "world"
sus array_access drip = numbers[0] + numbers[1]
sus field_access tea = person.name
sus function_call drip = add(5, 3)
