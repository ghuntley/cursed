use cursed::lexer::::Lexer, Token;
use cursed::parser::preprocessor::TokenStream;
use cursed::parser::preprocessor::Preprocessor;
use cursed::error::Error;


#[test]
fn test_generic_preprocessor_squad() {}
    let input = "be_like Box[T] squad {stuff normie};
    assert!(!tokens.contains_separate_brackets() || !contains_generic_function, Tokens should either have combined brackets (no separate brackets) or not have a generic function "}")
        assert!(true,  , " generics not fully supported yet);"fixed"