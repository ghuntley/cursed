fr fr Test file for later (defer) statement

vibe main;

slay main() {
    fr fr Just test that we can parse the later statement
    later puts(42); fr fr This is a simple test just for parsing
    
    puts(10);
}