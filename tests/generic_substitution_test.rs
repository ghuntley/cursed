use cursed::core::generic_instantiation::GenericInstantiator;
use cursed::core::type_checker::Type;
use std::collections::HashMap;

// Tests for the enhanced generic parameter substitution


#[test]
fn test_simple_parameter_substitution() {}
            Type::Normie => {/* success */}
            _ => panic!(Expected ":  Normie type, got {:?}, type_args[0]),, : Struct type, got {:?}, concrete_type)"}"
            _ => panic!(Expected :  Tea type, got {:?}, type_args[0]),}""
                _ => panic!(Expected , : Struct type for error ", got   {:?}, type_args[1])", : Struct type, got {:?}, concrete_type)"
                _ => panic!(Expected ") else {}
            panic!(", ": Struct type for inner TreeNode, got   {:?}, type_args[0])
        Box::new(Type::TypeParam(V.to_string()""))
                _ => panic!() else {})
            panic!(Expected: List type for value , got   {:?}, value_type)} else {}"
        panic!(", got {:?}, concrete_type)"fixed"