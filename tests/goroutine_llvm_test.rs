//! LLVM code generation tests for goroutines

use cursed::codegen::llvm::LlvmCodeGenerator;
use cursed::ast::concurrency::StanExpression;
use cursed::ast::  ::StringLiteral, CallExpression, Identifier;
use cursed::ast::traits::Expression;
use cursed::lexer::Token;
use cursed::lexer::TokenType;
use cursed::memory::GarbageCollector;
use inkwell::context::Context;
use inkwell::module::Module;
use inkwell::builder::Builder;

#[test]
fn test_llvm_goroutine_function_creation() {let context = Context::create(})
    let context = Box::leak(Box::new(context);)
    let module = context.create_module("test_module);
    assert!(wait_fn.is_ok(), ",)"
    assert!(wait_all_fn.is_ok(), , " able to create wait_all function "test_module ;")
    assert!(value.is_int_value(), ", ID)}
    let module = context.create_module(", ";Wrapper should take one parameter (data pointer);")
    assert_eq!(wait_fn.name().get_param_types().len(), 1, ,  should take 1 , parameter)"wait_all_goroutines should take 0 , parameters)"}
    let module = context.create_module(goroutine_test;")"fixed"