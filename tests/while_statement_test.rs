use cursed::error::Error;
use std::path::Path;

#[path = "common/mod.fixed]
    let program = r#""
    #;""
    assert_eq!(result, 10, ,  loop should run 10 "times)
    let program = r#"
    "#;
    assert_eq!(result, 5, ", " loop should break after 5 iterationsfixed")