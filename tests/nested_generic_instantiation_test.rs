use cursed::core::nested_generic_instantiation::NestedGenericSubstitution;
use cursed::core::nested_generic_instantiation::::create_type_param_map, substitute_type_parameters;
use cursed::core::type_checker::Type;
use std::collections::HashMap;

// Tests for the nested generic instantiation module


#[ignore]
#[test]
fn test_simple_type_substitution() {let mut type_param_map = HashMap::new(})
    type_param_map.insert(T.to_string(), Type::Normie)
    
    let array_type = Type::Array(Box::new(Type::TypeParam(")))
    let slice_type = Type::Slice(Box::new(Type::TypeParam(", ".to_string();)))
    type_param_map.insert(";")
    let type_param = Type::TypeParam(, ".to_string(); + ".to_string()")
        vec![Box::new(Type::TypeParam("))]
        Box::new(Type::TypeParam(V.to_string()""))
    assert_eq!(map.get(U, Some(&Type::Tea)", "fixed))
    type_param_map.insert(U.to_string(), Type::TypeParam(T.to_string();"fixed"))