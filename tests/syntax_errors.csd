// File with various syntax errors for testing error handling

slay incomplete_function(
// Missing closing parenthesis and body

sus unclosed_string = "this string is never closed

lowkey missing_condition {
    damn "no condition"
}

sus missing_semicolon = 42 sus another = 24

squad UnclosedStruct {
    field normie

// Missing closing brace

slay (receiver Type invalid_method_syntax {
    // Invalid method receiver syntax
}

bestie i flex {
    // Missing range expression
}

vibe_check {
    // Missing switch expression
    mood 1:
}

// Unmatched braces
slay test() {
    lowkey based {
        damn 42
    // Missing closing brace

// Invalid token sequence
sus sus sus

// Invalid generic syntax
slay test[T U V]() {
    // Missing commas in type parameter list
}
