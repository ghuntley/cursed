// Test for user-defined type methods

// Define a simple Vector2D type
Vector2D{x: 10, y: 20}

// Call methods on the Vector2D type
Vector2D.length() // Should calculate length
Vector2D.add(5, 5) // Should add values to x and y
Vector2D.toString() // Should return a string representation