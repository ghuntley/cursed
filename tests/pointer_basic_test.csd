slay main() {
    fr fr Test basic pointer operations
    sus x = 42;
    sus ptr = @x;       fr fr Get a pointer to x
    sus y = @ptr;      fr fr Dereference the pointer to get the value
    
    fr fr Print the result
    puts(y);
    
    damn 0;
}