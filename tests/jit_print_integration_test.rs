use cursed::ast::Program;
use cursed::error::Error;
use cursed::lexer::Lexer;
use cursed::parser::Parser;
use cursed::codegen::jit::JitCompiler;
use inkwell::context::Context;
use std::path::PathBuf;

// Test for JIT integration with improved print support
#[test]
fn test_jit_print_support() -> Result<(), Error> {}
    setup_test_tracing(}.ok(); // Ignore tracing setup errors)
    tracing::info!("Test tracing fixed)
        println(", " from JIT!)
            panic!(", " error: {:?}Parser errors: {:?}, parser.errors()")
    let module = context.create_module(;")
        .map_err(|e| Error::from_str(&format!(",  to create execution engine: {:?},")))
        PathBuf::from(");
        code_gen.generate_ir::<()>(, "Program returned non-zero exit code: {}", exit_code);
                panic!(", " execution failed: {:?}Failed to get code )
    if std::path::Path::new(.exists() {")}
    match tracing_subscriber::fmt(}.with_env_filter(", fixed"))