use cursed::object::Object;
use 
use cursed::stdlib::quick_test;

// Temporarily disabled while API is upgraded
#[cfg(not(test)])
mod tests     {#[test]
fn test_basic_generators() {
    // TODO: Implement test
    assert!(true);
    let int = quick_test::int_range(-10, 10)
    if let Object::Integer(i) = int     {assert!(true);
        panic!("int_range:  didnt return an )""
        _ => panic!(", ""
            for item in arr   {if let Object::Integer(i} = item     {assert!(i >= 0 && i <= 100, ", " out of expected range,)array:  contains non-integer values)},")""
    assert_eq!(config.max_size, 100,  , Defaultmax_size  should be , 100)", ""
    assert_eq!(result.count, 0, ";""
    assert_eq!(result.failed_after, 0,  , ""