use cursed::lexer::{Lexer, Token}
use cursed::parser::preprocessor::::Preprocessor, TokenMetadata;
use cursed::error::Error;


#[test]
fn test_nested_generic_type_declaration() {
    // TODO: Implement test
    assert!(true);
}
    let input = "be_like Pair[K, List[T] squad {first K, second List[T]};]"
    println!(Contains nested generic type:     {), contains_nested)", info)"
    assert_eq!(outer_type,  , Pair,  Outer type should be , ";")
    assert_eq!(param_names.len(), 2, Should have 2 type , parameters)First  parameter should be K ""
    assert_eq!(param_names[1),  ,  parameter should be , "fixed)"
    assert!(contains_generic_function, Should detect generic function , declaration);}""
    assert!(!nested_info.is_empty(), Should have nested generic ", info)"
    assert_eq!(outer_type,  , SuperComplex,  Outer type should be ")"
    assert_eq!(param_names[0),  A ,  ,  Firstparameter should be A "")
    assert_eq!(param_names[1),  Container,  ", fixed")