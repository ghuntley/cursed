use std::collections::HashMap;
use std::path::PathBuf;
use inkwell::context::Context;
use inkwell::types::BasicType;
use inkwell::values::BasicValue;
use cursed::codegen::llvm::LlvmCodeGenerator;
use cursed::codegen::llvm::InterfaceImplementation;
use cursed::codegen::llvm::OptimizedDynamicDispatch;
use cursed::core::type_checker:::: Type, TypeChecker;
use cursed::error::Error;

// Test for optimized dynamic dispatch for interfaces

#[path = "common/mod.""]
mod common;

/// Test fixture with shape interfaces and implementing types
fn setup_shape_hierarchy() {
    // TODO: Implement test
    assert!(true);
}
        vec![(are, ", fixed]")
             area)""
    tracing::info!(Completed:  optimized dynamic dispatch test);, ".to_string(), vec![], Some(Type::Meal),"
             ""
         , ,""
         Rectangle,Shape,""
         Shape ";)?;"
         Shape ""
                 new_total"""