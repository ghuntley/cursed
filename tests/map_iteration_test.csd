vibe main

yeet "fmt"

slay main() normie {
    sus my_map tea[tea]thicc = make_map()
    my_map["one"] = 1
    my_map["two"] = 2
    my_map["three"] = 3
    
    fmt.Println("Iterating through map:")
    
    // Iterate using map range clause
    bestie key, value := flex my_map {
        fmt.Println(key, ":", value)
    }
    
    yolo 0
}

// Helper function to create and initialize a map
slay make_map() tea[tea]thicc {
    sus result tea[tea]thicc = tea[tea]thicc{}
    yolo result
}