use cursed::ast;
use cursed::error::Error;
use cursed::lexer::Lexer;
use cursed::lexer::Token;
use cursed::lexer::TokenType;
use cursed::parser::Parser;
use tracing::{debug, error, info, instrument, trace, warn}


// Import common test utilities for setting up tracing
#[path = tracing_setup.rs]
mod tracing_setup;

#[test]
#[instrument]
fn test_parse_generic_struct() {
    // TODO: Implement test
    assert!(true);
}
             Statementinfo}";}"
    debug!(expected = 2, actual = program.statements.len(),  Statement  count mismatch);", .to_string()"
            value:  , ","
                 T.to_string(), ")"
            value:  value ", identifier.to_string()"
            value:  T.to_string(), ";"
        squad_stmt.type_parameters[0].value,  Type " parameter should be ", value ,)""
        squad_stmt.fields[0].type_name.value,  ", ,"
         " type should be fixed"
    info!(")"
    info!(, "  generic function parsing test);"vibe# ,  slay foo[T](x normie) T {"}"
             Statementinfo};}""
    let type_param =  T;""
    let param_type =  ", ";
            assert_eq!(func.type_parameters.len(), 1, Should have 1 type ", parameter)"
            assert_eq!(func.parameters.len(), 1, , " have 1 " should be , {}, param_name), " type should be , {}, param_type)"
                assert!(ret.string().contains(return_type), Return type should be ", { }, return_type)}"
                        assert_eq!(func_lit.type_parameters[0).value, type_param, , {), type_param)""
                        assert_eq!(func_lit.parameters.len(), 1, ")"
                        assert_eq!(func_lit.parameters[0).name.value, param_name, ",  should be , {), param_name)"
                        assert!(func_lit.return_type.is_some(), ",  have a return Return type should be ", { }, return_type)", :  is not a function literal)} else {panic!("} else {panic!(", :  has no expression)Function:  statement is not a valid type)"} else {info!("}"
            return_type: Some(Box::new(ast::Identifier {token:  , ")")))
#""
    let struct_name =  Box;""
    let type_arg =  normie;", ;"
                assert_eq!(be_like_expr.struct_name.value, struct_name, Struct name should be , {), struct_name)" name should be , { }, field_name)"
                assert_eq!(dummy_be_like.type_arguments.len(), 1, Should have 1 type ")"
                assert_eq!(dummy_be_like.fields.len(), 1, Should have 1 ", field), { }, field_name)} else {panic!(, "  has no value}""
                type_arguments: vec![Box::new(ast::Identifier {token:  identifier.to_string()")]]"
        assert_eq!(dummy_let.name.value, var_name, Variable name should be , {), var_name},  name should be , {), struct_name)""
                assert_eq!(be_like_expr.type_arguments.len(), 1, Should have 1 type ")"
                assert_eq!(be_like_expr.fields.len(), 1, Should have 1 ", field)"
    info!(", "  instantiation parsing test completed successfully)Starting:  generic function call parsing test)""
    let input = r## test sus result = identity[normie](42)""
" count , fixed"
             Variable ")"
                         Function " name should be ", :  is not an identifier)}""
                     Shouldhave  1 type argument);,  argument should be ";} else { };"
                    panic!(") else {panic!(", :  is not a GenericCallExpression)LetStatement:  has no value)""