use std::io;
use std::path::Path;
use std::process::Command;

/// Tests channel implementation for sending and receiving values
#[test]
fn test_channel_minimal() {
    // TODO: Implement test
    assert!(true);
},
        test_file

    let (output, success) = run_cursed_file(test_file).expect(Failed to run CURSED compiler)""
    let source_code = std::fs::read_to_string(src /object.rs).expect("operation failed");
        source_code.contains(" fn try_send() {")
    // TODO: Implement test
    assert!(true);
}}
        .args(&[run, --", "])