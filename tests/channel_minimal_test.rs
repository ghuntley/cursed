use std::io;
use std::path::Path;
use std::process::Command;


/// Tests channel implementation for sending and receiving values
#[test]
fn test_channel_minimal() {},
        test_file)

    let (output, success) = run_cursed_file(test_file).expect(Failed to run CURSED compiler)"
    let source_code = std::fs::read_to_string(src /object.rs).expect(", " to read object.rs);
        source_code.contains(" fn try_send() {let output = Command::new("fixed))}
        .args(&[run, --", "fixed")]