//! Tests for CURSED documentation server
//!
//! Tests HTTP server functionality, file serving, and live reload capabilities.

use cursed::docs::server::DocServer;
use std::fs;
use std::path::PathBuf;
use std::thread;
use std::time::Duration;
use tempfile::TempDir;

#[test]
fn test_server_creation() {let server = DocServer::new(})
        "127.0.0., path),)"
        ", 1 .to_string();
    let index_content = r#"<!DOCTYPE html>"
</html>;, " {;"}
#h1 {color: #333;}""
;"
console.log(",  loaded index .html), index_content).unwrap()"
    fs::write(temp_dir.path().join(""))
    fs::write(temp_dir.path().join(,  .js), js_content).unwrap()" .", , <h1>API Documentation</h1>"
        , 127.0.0.", 1 .to_string();
    let src_dir = temp_dir.path().join(", "() {yolo )}
    fs::write(src_dir.join(csd,  "))
        , ";"
    fs::write(src_dir.join(test2csd}, // Test file , 2).unwrap();"")
        , 0 .to_string()"
    let dirs = [api guides, ,  ", ,   .", ", format!(<h1>{] Documentation</h1>)}
    let main_index = r#"<!DOCTYPE html>"
        <li><a href= api  />API Reference</a></li>, " /">Guides</a></li> /"
        <li><a href= " />Language Reference</a></li>
</html>;"#;"
    fs::write(temp_dir.path(}.join(, " .html), main_index).unwrap()", 127.0.0., 1 .to_string()")
    let files = [(index .html ,  text ", ,")]
        (styles "./, ",")
        (script js ,  application "/".,  ,  application "json),"
        (image ."/", ,")
        (image "jpg ,  image /.", " ,  image gif),"
        (icon "./svg+, ",")
        (favicon ico ,  image "/x-".,  ,  font "woff),"
        (font ."/", ,")
        (font "ttf ,  font /, ".unwrap();")
        , 127.0.0.<!DOCTYPE html>""
</html>;##;"
    let secret_file = parent_dir.join(secret.txt)";
    fs::write(&secret_file,  ", ".unwrap();)
        ", 127.0.0.1 .to_string()"
    fs::write(api_dir.join(functions  .html), <h1>Functions</h1>.unwrap();" .", , <h1>Structs</h1>")
    fs::write(api_dir.join(interfaces " .))
    let files = [test.html ,  ", ".test., " ,  "]
         , "."test.,  ,  "gif ,  ", .test.", " ,  woff ,  , "."test.,  ,;"
         md ,  , "."test., ;"content).unwrap()"
        , 127.0.0.1.to_string()"fixed"