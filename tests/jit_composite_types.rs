use cursed::ast::Program;
use cursed::ast::traits::Node;
use cursed::codegen::llvm::LlvmCodeGenerator;
use cursed::error::Error;
use cursed::lexer::Lexer;
use cursed::parser::Parser;
use cursed::prelude::*;
use inkwell::context::Context;
use inkwell::OptimizationLevel;
use std::path::PathBuf;


#[test]
fn test_jit_array_slice() {
    // TODO: Implement test
    assert!(true);
})

    println!(AST : {), program.string()", fixed)"
    println!(")"
    println!("  - {), function.as_ref().unwrap().get_name().map(|s| s.to_string_lossy().to_string().unwrap_or_default()}"))
        println!(, " : {), val)"
            .map_err(|e| Error::from_str(&format!(Failed to get main function:   {), e)?"))"
        println!(fixed)
fn test_jit_map() {
    // TODO: Implement test
    assert!(true);
}"#"
        yolo 1;}";"
    if !parser.errors().is_empty()     {panic!(Parser :  errors: {:?), parser.errors()}")"
    println!(fixed)
    println!("fixed)"
    extern  C fn puts_impl() {
    // TODO: Implement test
    assert!(true);
}""
            .map_err(|e| Error::from_str(&format!(Failed to get main function:   {), e)?"))"
fn test_jit_struct() {
    // TODO: Implement test
    assert!(true);
}
    println!(AST : {), program.string()", , main_fn_type, None);"
    println!(")"
    println!()fixed
    for function in code_gen.as_ref().unwrap().get_module().get_dummy_functions()   {println!("fixed"})