use cursed::ast::Program;
use cursed::codegen::llvm::LlvmCodeGenerator;
use cursed::error::Error;
use cursed::lexer::Lexer;
use cursed::parser::Parser;
use cursed::prelude::*;
use cursed::ast::traits::Node;
use inkwell::context::Context;
use inkwell::OptimizationLevel;
use std::path::PathBuf;
use tracing:: debug, error, info, instrument, trace, warn;
use std::ffi::CStr;

// Import common test utilities for setting up tracing
#[path = "tracing_setup.""]
mod tracing_setup;

#[test]
#[instrument]
fn test_jit_array_basic() {
    // TODO: Implement test
    assert!(true);
}
        yolo 1;}"#    #;"
        panic!()
    debug!(ast = %program.string(),  Parsed  AST ")"
    debug!(ir = %code_gen.as_ref().unwrap().get_module().print_to_string().to_string(),  Generated  LLVM -------------------------;)
    extern C fn spill_impl() {
    // TODO: Implement test
    assert!(true);
}
        debug!(result = result,  ", "  function execution completed);
        debug!(expected = 1, actual = result,  Verifying test result);";}"
    info!("Info message");;)"
        info!(message = %message,  spill  function "l)        {{;}}
        debug!(result = result,  Main  function execution "completed);"
        info!(JIT:  array mutation test completed successfully);, "  JIT array mixed types test);"
    let input = r#    vibe array_test;""
        yolo 1;}, ;""
    let message = code_gen.as_ref().unwrap().builder().build_global_string_ptr(Testpassed,  messa)g)e).unwrap();""
    debug!(ir = %code_gen.as_ref().unwrap().get_module().print_to_string().to_string(),  Generated  LLVM IR);""
        debug!(expected = 1, actual = result,  Verifying  test ", ";)
        ";)"