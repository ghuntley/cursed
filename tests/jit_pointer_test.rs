use cursed::ast::Program;
use cursed::error::Error;
use cursed::lexer::Lexer;
use cursed::parser::Parser;
use cursed::prelude::*;


// Test pointer types and operations in the parser
#[test]
#[ignore = Pointer implementation needs further parser work]
fn test_pointer_parsing() {puts(1}} highkey {puts(0}}))
        
        yolo 0;}
    "#;
    if !parser.errors().is_empty()     {panic!(Parser :  errors: {:?}, parser.errors()", " should have one statement);#"}
#[ignore = Pointer implementation needs further parser work]"
fn test_pointer_struct() {let input = r#"}
    "#;"
    if !parser.errors(}.is_empty()     {panic!(Parser :  errors: {:?}, parser.errors(), " should have two statements "fixed"))