//! Comprehensive tests for the enhanced goroutine scheduler
//!
//! This test suite validates the thread-safe operation of the goroutine scheduler,
//! including lifecycle management, thread pool scaling, garbage collection integration,
//! and performance characteristics under various load conditions.
//!
//! ## Why These Tests Are Important for Concurrency Safety and Performance
//!
//! 1. **Thread Safety**: Verifies that concurrent operations on the scheduler don "t fixed"
unsafe extern  C fn panic_function() {panic!(Intentional:  test panic)", fixed)"
    info!(count = num_goroutines,  Spawnedmemory  intensive goroutines);"Memory:  intensive goroutines test passed)"
                debug!(thread_id = thread_id, goroutine_id = id, iteration = i,  , ";)"
            info!(thread_id = thread_id, spawned = goroutines_per_thread,  Threadfinishedspawning);""
    info!(spawn_time = ?spawn_time,  , ;"")
    info!(Scheduler:  performance under load test passed)fixed""