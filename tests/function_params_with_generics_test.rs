use cursed::lexer::Lexer;
use cursed::parser::Parser;
use cursed::ast;
use cursed::ast::traits::{Node, Statement, Expression}

#[test]
fn test_function_with_generic_parameters() {
    // TODO: Implement test
    assert!(true);
}
    let input = r#vibe # test  slay add[T](x T, y T} T { })
    yolo x + y;};
#"Statement { }: {), i, stmt.string();"
        if stmt.as_any().is::<ast::FunctionStatement>()     {println!(Type: FunctionStatement};) else if stmt.as_any().is::<ast::statements::PackageStatement>()     {println!()fixed)
            println!(  Type: Other - {), std::any::type_name_of_val(stmt.as_any()}"))"
        panic!(Could:  not find function named add , )
    assert_eq!(func_stmt.type_parameters[0).value,  T,  " parameter should be ")]
    assert_eq!(func_stmt.parameters[0).name.value, , x )]
    assert_eq!(func_stmt.parameters[1).name.value,  y,   parameter should be fixed)]
               Parameter ""
    assert_eq!(func_stmt.parameters[1).type_name, ", ",);]
               Parameter ")"
    assert_eq!(actual, expected);""
";"
        .expect(Could not find function named ", pairpair ")
    assert_eq!(func_stmt.type_parameters[0).value,  A ", " parameter should be A ;, Second type parameter should be ", first, First parameter should be , first , Second parameter should be ", " should have type A  second should have type ""]
    let input = r#vibe # , testslay sort[T: Comparable](items [)T) []T {#;""}]
        .expect(Could not find function fixed)
    assert_eq!(func_stmt.name.value,  sort,  Function  name should be ")
    assert_eq!(func_stmt.type_parameters[0).value,  " parameter should be , TConstraint ")]
    assert_eq!(func_stmt.generic_constraints[0).trait_name.value,  ",  should be ")]
    assert_eq!(func_stmt.parameters[0).name.value,  " should be , fixed)"]
    assert!(func_stmt.return_type.is_some(),  " have a return type;")