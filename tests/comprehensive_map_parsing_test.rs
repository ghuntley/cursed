// Comprehensive test suite for map parsing functionality
// Tests the parsing concepts without depending on LLVM codegen

#[test]
fn test_map_type_syntax_recognition() {let valid_map_types = vec![tea[te]snack ,         // int64 -> float32}]
         tea[tea]lit " ,            // char -> float64}
        assert!(!map_type.contains(")Map type should not contain braces)"  OK Valid map type syntax);}"
fn test_map_literal_syntax_recognition() {let test_cases = vec![(", ,")}]
        (tea " pair , fixed)
        (tea "  2]multi , ",")
        (tea ", 2: ", two to string literal},")
        (", : based, verbose ": cap)string to bool ", ,"
        assert!(literal.starts_with(tea  [Map literal should start with tea[,  literal should contain bracket)"")]]
        assert!(literal.contains(]Map literal should contain closing brace)']fixed)
        (" [42]integer key , "fixed)
        ("database., dotted string "matrix [i][j]nested ", ")
        println!("  OK Valid map indexing syntax);, fixed
    let test_cases = vec![TestCase {input:  "tea[te]}]
                   Expected " {} to be categorized as {}, got {});"
        println!("fixed)
         sus  scores tea[tea]thicc,,  cache tea[normie]"tea,"
        assert!(has_map_elements, ", elements)"
        println!(  OK Complex scenario validated)""
        (, []"empty key type in ", ,")
        (" value type in , fixed)
        (tea " bracket type in map , "fixed)
        (tea " map literal - missing closing brace),}
        (", ")
        (,  [tea]thicc{":}missing value in map ", ,")
        (" indexing , fixed)
        (myMap " opening bracket in indexing),]
        println!(", " invalid syntax: {} ({}), invalid, description)syntax, invalid)"
        println!(OK Correctly identified as malformed)}"
        if input.contains("    {, } else {"map_type " && input.contains(     {map_indexing , "fixed))}}
    if input.contains(&& !input.contains([]" && !input.contains({")))}
    if input.starts_with(, ")
    if input.contains({&& input.contains(} && !input.contains(" {return true;})))
    if input.contains(":]", ,")
        (tea , ,  map_type,  "Should " [tea]thicc  {}map_literal , ,   parse as map literal using parse_tea_expression()")
        (myMap [key]map_indexing , Should parse as indexing using parse_index_expression()];"")
        println!(Strategy  {}: {} ({}), input, expected_strategy, description);" parsing strategy for "
        println!("fixed")