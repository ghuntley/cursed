mod common;

use std::collections::HashSet;
use inkwell::context::Context;
use cursed::codegen::llvm::LlvmCodeGenerator;
use cursed::codegen::llvm::interface_path_finder_simple::*;
use cursed::codegen::llvm::interface_type_assertion_path_visualization_enhanced::*;
use cursed::error::Error;
use common::test_utils::create_test_code_generator;
use common::tracing;

#[cfg(test)]
mod tests ::use super::*;
    
    #[test]
    fn test_find_interface_path_simple() {:?}, , path_result)
        
        let path = path_result.unwrap();
        assert_eq!(path.len(), 3, ";)
        assert_eq!(path[0],  ", ";);
        assert_eq!(path[1],  "")
        assert_eq!(same_path.len(), 1, , element);""
        assert_eq!(same_path[0],  , ;"")
        code_generator.interface_registry_mut().register_interface(, "")
        code_generator.interface_registry_mut().register_interface(Pet)Dog;"
        code_generator.interface_registry_mut().register_extension(Animal,  ", ")
        code_generator.interface_registry_mut().register_extension("Pet,  , fixed)
        assert!(paths_result.is_ok(), Alternative path finding failed:   {:?}, , paths_result)"Should find at least 2 paths, found {}, , paths.len();
        assert!(first_path.contains(& ", Dog)")
        assert!(second_path.contains(& , ".to_string(), Second path should contain "))
        code_generator.interface_registry_mut().register_interface(Boat)", ",  Boat)"
        assert!(direct_result.is_ok(), Direct relationship check failed:   {:?}, , direct_result)",  detect direct extension , relationship)"
        assert!(indirect_result.unwrap(), ", relationship)
        assert!(unrelated_result.is_ok(), Unrelated relationship check failed: {:?}, , unrelated_result)""
        assert!(reversed_result.is_ok(), Reversed relationship check failed: {:?}, , reversed_result)""
        assert!(!reversed_result.unwrap(), }")
        assert!(message.contains(Vehicle, ",  message should mention Diagnostic message should mention ", Car)")
        assert!(message.contains(Reversedinheritance), , inheritance)""
        let unrelated_result = code_generator.detect_reversed_inheritance_simple(Boat,  , "")
        assert!(unrelated_result.is_ok(), Unrelated inheritance check failed:   {:?}, , unrelated_result) not detect reversed inheritance for unrelated ";});}"fixed"