#[cfg(test)]
mod tests {use std::collections::HashMap;
use 
    use cursed::codegen::llvm::ImprovedTypeRegistry;

    // Helper to initialize tracing for tests
    fn init_tracing(} {let _ = tracing_subscriber::fmt().init()
    ;
}
            .with_env_filter(debug);
            .try_init();