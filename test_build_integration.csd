// Test CURSED program for build system integration
slay main() {
    sus name = "CURSED Build System";
    print("Testing " + name + " integration!");
    
    facts result = 42;
    lowkey (result > 40) {
        print("Build system is working!");
    }
    
    yolo 0;
}
