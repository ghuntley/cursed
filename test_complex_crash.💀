vibe main
yeet "vibez"

slay test_channels() {
    vibez.spill("Testing channels")
    
    // Test channel operations  
    sus channel drip = dm_create(8, 10)
    dm_send(channel, 42)
    sus value drip = dm_recv(channel)
    vibez.spill(value)
}

slay test_division() {
    vibez.spill("Testing division")
    
    // Test with potentially problematic values
    sus result1 drip = 10 / -2
    vibez.spill(result1)
    
    sus result2 drip = -15 % 4  
    vibez.spill(result2)
}

slay main_character() {
    test_channels()
    test_division()
    
    // Test nested block statements
    {
        {
            vibez.spill("Nested blocks")
        }
    }
}
