vibe main

yeet "vibez"
yeet "mathz"

slay math_with_simple_vars() normie {
    sus a normie = mathz.abs(-42)
    sus b normie = mathz.max(10, 20)
    sus result normie = a + b
    damn result
}

slay main_character() {
    vibez.spill("Testing simple math")
    sus result1 normie = math_with_simple_vars()
    vibez.spill(result1)
}
