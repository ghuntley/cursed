// Test very simple generic function
// Just to verify the parser can handle the angle brackets

// Simple function with generic type parameter
slay identity<T>(value T) T {
    damn value
}

// Test the function
vibez.spill("Testing simple generic function")
sus result := identity<normie>(42)
vibez.spill("Result: " + result.to_string())
