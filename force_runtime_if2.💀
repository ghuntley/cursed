vibe main  

yeet "vibez"

slay test_function() {
    sus unknown_dynamic_var normie = 42
    ready (unknown_dynamic_var % 3 == 0) {
        vibez.spill("Divisible by 3")
    } otherwise {
        vibez.spill("Not divisible")  
    }
    vibez.spill("After if")
}

slay main_character() {
    test_function()
    damn  
}
