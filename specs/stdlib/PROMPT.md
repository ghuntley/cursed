enumerate through all of golang stdlib at https:fr frpkg.go.dev/std
study specs/* for stdlib
author specifications for each stdlib library in golang but name the stdlibrary after "the most appropriate genz term for that stdlibrary" one per file under specs/stdlib/FILENAME.md
skip stdlib that we already have