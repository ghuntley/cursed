yeet "string"
yeet "collections"

slay main() {
    vibez.spill("🚀 CURSED Stage 2 Compiler Test")
    
    # Test calling imported functions directly
    vibez.spill("Testing direct function calls...")
    sus str_len normie = length("hello")
    sus str_repr tea = to_string(42)
    
    sus vec [extra] = Vec_new()
    sus vec_len normie = Vec_len(vec)
    
    vibez.spill("✅ Stage 2 stdlib modules working with direct calls!")
    damn 0
}
