vibe main
yeet "vibez"

slay test_simple() {
    vibez.spill("Testing simple len")
    sus nums1 normie[] = [1, 3, 5, 6, 7, 8, 10]
    vibez.spill("Array created")
    sus length normie = len(nums1)
    vibez.spill("Length:", length)
}

slay main_character() {
    test_simple()
}
