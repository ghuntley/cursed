when you need to use git use it via the bash tool
update IMPLEMENTATION_STATUS.md and NEXT_STEPS.md
add changes and IMPLEMENTATION_STATUS.md and NEXT_STEPS.md with "git add -A"
then do a "git commit" with a message that describes the changes you made to the code.
then do a "git push" to push the changes to the remote repository.
