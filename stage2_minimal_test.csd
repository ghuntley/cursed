yeet "string"
yeet "collections"

slay main() normie {
    vibez.spill("🚀 CURSED Stage 2 Compiler Test")
    
    # Test string module
    sus test_str tea = "Hello"
    sus str_len normie = string.length(test_str)
    vibez.spill("String length works")
    
    # Test collections
    sus test_vec [extra] = collections.Vec_new()
    vibez.spill("Collections work")
    
    vibez.spill("✅ Stage 2 stdlib modules working!")
    damn 0
}
