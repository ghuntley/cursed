slay divide(x normie, y normie) normie {
    lowkey y == 0 {
        vibez.spill("Error: Division by zero");
        yolo -1;
    }
    yolo x / y;
}

slay main() {
    sus result1 = divide(10, 2);
    sus result2 = divide(10, 0);
    vibez.spill("Division tests complete");
    yolo result1;
}
