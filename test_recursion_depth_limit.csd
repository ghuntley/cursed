slay infinite_recursion(n normie) normie {
    vibez.spill("Recursion depth:");
    vibez.spill(n);
    yolo infinite_recursion(n + 1);
}

slay main() {
    vibez.spill("Testing recursion depth limit...");
    yolo infinite_recursion(1);
}
