vibe main
yeet "collections"
yeet "vibez"

slay main_character() {
    vibez.spill("Testing collections module loading...")
    vibez.spill("Calling collections.length function:")
    
    fr fr Simple array literal for testing
    sus test_array normie[value] = {1, 2, 3, 4, 5}
    sus len normie = collections.length(test_array)
    vibez.spill(len)
}
