vibe main

slay main() {
    fr fr Test basic tuple functionality
    sus my_tuple = (42, "hello", based);
    
    fr fr Test tuple access
    sus first = my_tuple.0;
    sus second = my_tuple.1;
    sus third = my_tuple.2;
    
    vibez.spill("First element:");
    vibez.spill(first);
    vibez.spill("Second element:");
    vibez.spill(second);
    vibez.spill("Third element:");
    vibez.spill(third);
    
    fr fr Test tuple destructuring
    sus coordinates = (100, 200);
    sus (x, y) = coordinates;
    
    vibez.spill("X coordinate:");
    vibez.spill(x);
    vibez.spill("Y coordinate:");
    vibez.spill(y);
    
    yolo 0;
}
