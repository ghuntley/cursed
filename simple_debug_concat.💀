vibe main
yeet "vibez"
yeet "stringz"

slay main_character() {
    sus a = "AB"
    sus b = "CD"
    vibez.spill("A:", a)
    vibez.spill("B:", b)
    sus result = stringz.concat(a, b)  
    vibez.spill("Result:", result)
    vibez.spill("Length:", stringz.length(result))
}
