// Debug exec_vibez module to isolate parsing issues

facts EXEC_SUCCESS = 0

sus current_command tea = ""
sus last_exit_code normie = 0
sus last_success lit = based

slay exec_simple_test() lit {
    damn based
}

slay exec_set_command(name tea) lit {
    current_command = name
    damn based
}

slay exec_get_status() normie {
    damn last_exit_code
}
