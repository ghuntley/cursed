vibe vibez

slay spill(message tea) {
    fr fr Built-in print function - implementation provided by runtime
}

slay spill(value normie) {
    fr fr Built-in print function for integers - implementation provided by runtime
}
