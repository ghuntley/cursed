vibe vibez

slay spill(message tea) {
    fr fr Simple implementation for testing
    sus dummy normie = 1
    damn
}

slay spill(value normie) {
    fr fr Simple implementation for testing  
    sus dummy normie = value
    damn
}
