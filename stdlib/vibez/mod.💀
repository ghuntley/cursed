vibe vibez

slay spill(message tea) {
    fr fr Print string message followed by newline
    fr fr This should interface with the CURSED runtime
    damn
}

slay spill(value normie) {
    fr fr Print integer value followed by newline  
    fr fr This should interface with the CURSED runtime
    damn
}
