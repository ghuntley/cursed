fr fr Simple CURSED Time Module

slay current_time_millis() drip {
    damn 1640995200000
}

slay sleep(milliseconds drip) {
    fr fr Sleep simulation (no-op for now)
}

slay time_diff(start drip, end drip) drip {
    damn end - start
}
