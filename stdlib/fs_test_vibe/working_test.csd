yeet "testz"

fr fr Test without importing fs_test_vibe module
slay main() {
    test_start("Basic fs_test_vibe functionality") fr fr Test basic functionality without complex types
    assert_true(1 == 1)
    
    sus file_path tea = "test.txt"
    assert_eq_string(file_path, "test.txt")
    
    sus file_mode normie = 644
    assert_eq_int(file_mode, 644)
    
    assert_false(1 == 0)
    
    vibez.spill("✅ Basic fs_test_vibe functionality working!")
    print_test_summary()
}
