fr fr Simple CURSED I/O Module

slay print(message tea) {
    fr fr Print simulation (no-op for now)
}

slay println(message tea) {
    fr fr Println simulation (no-op for now) 
}

slay read_line() tea {
    damn "user_input"
}
