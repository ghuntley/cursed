yeet "string"
yeet "collections"

slay main() {
    vibez.spill("🚀 CURSED Stage 2 Compiler Test")
    
    # Test string module
    vibez.spill("Testing string module...")
    sus str_len normie = string.length("hello")
    
    # Test collections module  
    vibez.spill("Testing collections module...")
    sus vec [extra] = collections.Vec_new()
    
    vibez.spill("✅ Stage 2 stdlib modules working!")
    damn 0
}
