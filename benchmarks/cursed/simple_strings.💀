# Simple Strings Benchmark
slay main_character() drip {
    sus iterations drip = 1000  
    sus total drip = 0
    
    bestie (sus i drip = 0; i < iterations; i++) {
        sus str1 tea = "Hello"
        sus str2 tea = "World"
        sus combined tea = str1 + str2
        total = total + len(combined)
    }
    
    vibez.spill("Strings result:", total)
    damn 0
}
