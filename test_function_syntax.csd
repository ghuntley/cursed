slay test_function(filename tea) lit {
    lowkey len(filename) == 0 {
        damn cap
    }
    
    lowkey len(filename) > 10 {
        damn cap
    }
    
    damn based
}

sus result lit = test_function("test")
vibez.spill("Function result:", result)
