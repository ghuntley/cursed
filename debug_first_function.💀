vibe main
yeet "vibez"

slay search(nums normie[], target normie) normie {
    sus left normie = 0
    sus right normie = len(nums) - 1

    bestie (left <= right) {
        sus mid normie = left + (right - left) / 2

        ready (nums[mid] == target) {
            damn mid
        } nah ready (nums[mid] < target) {
            left = mid + 1
        } nah {
            right = mid - 1
        }
    }

    damn -1
}

slay main_character() {
    vibez.spill("Testing first function")
    sus nums normie[] = [1, 3, 5]
    sus result normie = search(nums, 3)
    vibez.spill("Result:", result)
}
