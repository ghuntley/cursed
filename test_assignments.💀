vibe main

yeet "vibez"

slay main_character() {
    sus x normie = 10
    vibez.spill("Initial x =", x)
    
    x = 20
    vibez.spill("After assignment x =", x)
    
    x = x + 5
    vibez.spill("After addition x =", x)
    
    damn
}
