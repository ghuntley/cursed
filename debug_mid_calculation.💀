vibe main
yeet "vibez"

slay main_character() {
    sus left normie = 0
    sus right normie = 5
    bestie (left <= right) {
        sus mid normie = left + (right - left) / 2
        vibez.spill("mid =", mid)
        left = left + 1
    }
}
