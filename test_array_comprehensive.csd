slay main() {
    sus numbers []normie = [10, 20, 30, 40, 50]
    vibez.spill("Array created with values: [10, 20, 30, 40, 50]")
    
    // Test multiple array indices
    sus first normie = numbers[0]
    sus second normie = numbers[1]
    sus third normie = numbers[2]
    
    vibez.spill("First element (numbers[0]):")
    vibez.spill(first)
    
    vibez.spill("Second element (numbers[1]):")
    vibez.spill(second)
    
    vibez.spill("Third element (numbers[2]):")
    vibez.spill(third)
    
    // Test with expression as index
    sus index normie = 3
    sus fourth normie = numbers[index]
    vibez.spill("Fourth element (numbers[index] where index=3):")
    vibez.spill(fourth)
    
    yolo 0
}
