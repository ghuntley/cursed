vibe main
yeet "vibez"

slay test_old_syntax() {
    fr fr This should fail with old @ syntax
    sus x normie = 42
    sus ptr @normie = @x
    
    vibez.spill("This should not work")
}

slay main_character() {
    test_old_syntax()
}
