study specs/*

Your task is determine if specs/* have been implemented in src/*.

- Create a bullet point list of items which have not been implemented.
- Sort them by priority (as in what is most important to get the compiler going)
- Also create a bullet list of items you have not _yet_ verified. We'll use this as a checkpoint of progress. Start with this one first. This checkpoint should be written to the IMPLEMENTATION_STATUS.md as well.

It's important that you verify the status by reading the files in "src/*". Do not make any assumptions, read the files as needed.

Continue with this process forever until you run out of context window space.

Use as many subagents as you need to use (up to 500)
