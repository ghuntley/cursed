yeet "arrayz"
yeet "stringz"

slay memory_stress_test() {
    sus results []drip = []
    sus i drip = 0
    bestie (i < 50) {
        sus temp_array []drip = [i, i * 2, i * 3]
        sus temp_string tea = "Test " + toString(i)
        results = results + [len(temp_array) + len_str(temp_string)]
        i = i + 1
    }
    damn results
}

sus final_results []drip = memory_stress_test()
vibez.spill("Memory stress test completed, results length:", len(final_results))
