fr fr This is a line comment
vibe main

slay greet(name: tea) -> tea {
    facts greeting = "Hello"
    sus result: tea
    lowkey (name == cap) {
        result = "Hello, Anonymous"
    } highkey {
        result = greeting + ", " + name
    }
    yolo result
}

no cap 
This is a block comment
spanning multiple lines
on god

slay main() {
    sus name = "World"
    facts message = greet(name)
    yeet_error message
}

sus based_value = based
sus sus_value = sus
facts cap_value = cap

sus number = 42
sus float_val = 3.14
