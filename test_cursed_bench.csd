# Simple test program for benchmarking

slay test_arithmetic() lit {
    sus result normie = 0
    bestie i := 0; i < 1000; i++ {
        result = result + (i * 2)
    }
    damn based
}

test_arithmetic()
vibez.spill("Arithmetic test completed")
