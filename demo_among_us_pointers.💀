vibe main
yeet "vibez"

// Among Us Pointer Syntax Demonstration
// =====================================
// CURSED uses the * and & for pointer operations
// This makes memory management sus in the best way possible!

slay main_character() {
    vibez.spill("🔥 Among Us Pointer Syntax Demo")
    vibez.spill("==============================")
    
    // Basic pointer declaration and usage
    sus value normie = 42
    sus ptr_to_value *normie = &value  // Create pointer to value
    
    vibez.spill("Original value:", value)
    vibez.spill("Pointer address:", ptr_to_value)
    vibez.spill("Dereferenced value:", *ptr_to_value)  // Dereference pointer
    
    // Modify through pointer
    *ptr_to_value = 69  // Among Us reference!
    vibez.spill("Value after pointer modification:", value)
    
    // Nested pointers (sus within sus)
    sus double_ptr **normie = &ptr_to_value  // Pointer to pointer
    vibez.spill("Double dereferenced value:", **double_ptr)
    
    // Array pointers
    sus numbers normie[] = [1, 3, 3, 7]
    sus array_ptr *normie = numbers  // Points to first element
    
    vibez.spill("First element via pointer:", *array_ptr)
    vibez.spill("Second element:", *(array_ptr + 1))
    
    // Function pointer example
    sus func_ptr *() = main_character
    vibez.spill("Function pointer address:", func_ptr)
    
    // Pointer arithmetic (sus math!)
    sus base *normie = numbers
    sus offset normie = 2
    sus element_ptr *normie = base + offset
    vibez.spill("Element at offset 2:", *element_ptr)
    
    // Among Us themed message
    vibez.spill("")
    vibez.spill("🚀 The Among Us pointer syntax is working!")
    vibez.spill("   * = Dereference pointer")
    vibez.spill("   & = Take address")
    vibez.spill("   This syntax is sus... but in a good way! 🔥")
}
