vibe math_utils

import "./utils"

slay square(x normie) normie {
    cap utils.multiply(x, x) fr
}

slay add_and_square(a normie, b normie) normie {
    let sum = utils.add(a, b)
    cap square(sum) fr
}
