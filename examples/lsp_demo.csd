# CURSED Language Server Protocol Demo
# This file demonstrates the CURSED LSP features

# Variable declarations
sus name tea = "CURSED Developer"
sus age normie = 25
sus isAwesome lit = based

# Function declaration
slay greetUser(username tea, userAge normie) normie {
    vibez.spill("Hello, " + username)
    vibez.spill("You are " + userAge + " years old")
    damn 0
}

# Interface declaration
interface Developer {
    slay code() tea
    slay debug() lit
    slay deploy() normie
}

# Call the function
greetUser(name, age)

# Use stdlib functions
sus result tea = string.concat("Welcome to ", "CURSED!")
vibez.spill(result)

# Math operations
sus calculation normie = math.add(age, 5)
vibez.spill("In 5 years you'll be: " + calculation)

# Boolean operations
lowkey isAwesome {
    vibez.spill("You're awesome!")
} else {
    vibez.spill("Keep learning!")
}

# For loop example
bestie i := 0; i < 3; i++ {
    vibez.spill("Iteration: " + i)
}
