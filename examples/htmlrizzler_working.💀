vibe main

yeet "vibez"
yeet "htmlrizzler"

slay main_character() {
    // Direct calls to our patched vibez.spill function
    vibez.spill("The htmlrizzler module is now properly loaded");
    vibez.spill("Function calls with dot expressions are now working correctly");
    
    damn 0;
}