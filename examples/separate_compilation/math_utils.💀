vibe mathutils;

fr fr Math utilities package for CURSED

slay add(a normie, b normie) normie {
    cap a + b
}

slay multiply(a normie, b normie) normie {
    cap a * b
}

slay double(n normie) normie {
    cap n * 2
}

slay triple(n normie) normie {
    cap n * 3
}
