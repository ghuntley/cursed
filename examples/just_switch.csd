vibe main

slay main_character() normie {
    normie x = 1;
    // Parse open brace separately
    vibe_check x 
    {
        // Case with explicit colon
        mood 1: 
            x = 10;
        // Default case
        basic: 
            x = 0;
    }
    damn 0;
}