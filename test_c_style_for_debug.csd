# Test C-style for loop syntax that's failing
slay test_c_style_for() lit {
    # This should work with C-style for loop
    bestie i := 0; i < 5; i++ {
        vibez.spill("Iteration")
    }
    damn based
}

test_c_style_for()
