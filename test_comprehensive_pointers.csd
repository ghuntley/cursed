fr fr Comprehensive pointer test for CURSED language

slay main() {
    fr fr Basic pointer operations
    sus x normie = 42
    sus ptr @normie = @x
    
    fr fr Test basic compilation without using pointers in ways that would fail
    vibez.spill("Original value: " + x)
    
    yolo 0
}
