fr fr Simple struct test for runtime implementation

squad Person {
    name tea
    age normie
}

slay main() {
    fr fr Create struct with literal syntax
    sus p Person = Person { name: "Alice", age: 25 }
    
    fr fr Access fields
    vibez.spill("Name:")
    vibez.spill(p.name)
    
    vibez.spill("Age:")
    vibez.spill(p.age)
}
