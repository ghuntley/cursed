fr fr Test complex CURSED program for AST parsing

squad Point {
    spill x normie
    spill y normie
}

collab Drawable {
    slay draw()
    slay area() meal
}

slay test_function(param normie) normie {
    sus local_var normie = 42
    
    if (param > 0) {
        yolo local_var * 2
    } else {
        yolo 0
    }
}

slay main() {
    sus p Point = Point{x: 10, y: 20}
    sus result normie = test_function(p.x + p.y)
    vibez.spill(result)
}
