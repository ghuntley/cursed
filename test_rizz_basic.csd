yeet "stdlib/rizz_template"

slay main() {
    vibez.spill("Testing rizz_template module...")
    
    sus tmpl := rizz_template.New("test")
    if tmpl != cringe {
        vibez.spill("✅ New template creation works")
    } else {
        vibez.spill("❌ New template creation failed")
    }
    
    sus cache := rizz_template.NewTemplateCache()
    if cache != cringe {
        vibez.spill("✅ Template cache creation works")
    } else {
        vibez.spill("❌ Template cache creation failed")
    }
    
    vibez.spill("Basic rizz_template tests completed!")
}

main()
