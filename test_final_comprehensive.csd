vibe main

be_like Person squad {
    name tea
    age normie
}

slay createPerson(name tea, age normie) Person {
    yolo Person { name: name, age: age }
}

slay main() {
    sus numbers []normie = [1, 2, 3, 4, 5]
    sus names []tea = ["Alice", "Bob", "Charlie"]
    
    vibez.spill("Array type parsing: WORKING")
    vibez.spill("Struct definitions: WORKING")
    vibez.spill("Function definitions: WORKING")
    
    sus person Person = createPerson("Test", 25)
    vibez.spill("Struct literals: WORKING")
    
    yolo 42
}
