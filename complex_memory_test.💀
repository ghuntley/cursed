vibe main
yeet "vibez"
yeet "mathz"

slay add_numbers(x drip, y drip) drip {
    damn x + y
}

slay main_character() {
    vibez.spill("Testing complex function calls")
    sus result drip = add_numbers(10, 32)
    vibez.spill("Result:", result)
    
    lowkey result > 40 {
        vibez.spill("Large number detected")
    }
    
    facts message tea = "Function call test complete"
    vibez.spill(message)
}
