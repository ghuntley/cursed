fr fr Direct stdlib test without module imports

outer slay runtime_print_string(message [*:0]normie)

slay spill(msg tea) lit {
    ready msg == cringe {
        damn cap
    }
    runtime_print_string(msg)
    damn based
}

slay spillln(msg tea) lit {
    ready msg == cringe {
        damn cap
    }
    runtime_print_string(msg)
    runtime_print_string("\n")
    damn based
}

slay main_character() {
    spillln("=== Direct CURSED Standard Library Test ===")
    
    sus x normie = 42
    sus message tea = "Hello CURSED"
    
    spillln("Basic test completed successfully")
}
