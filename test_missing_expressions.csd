slay test_missing_expressions() {
    // Test basic expressions that might be missing
    sus x drip = 42
    sus y meal = 3.14
    sus s tea = "hello"
    sus flag lit = based
    sus ch sip = 'A'
    
    // Test increment/decrement
    x = x + 1
    
    // Test array creation
    sus arr = [1, 2, 3]
    
    vibez.spill("Testing missing expressions")
}
