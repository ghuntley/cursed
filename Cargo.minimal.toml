[package]
name = "cursed"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "cursed"
path = "src/main.rs"

[dependencies]
clap = "4.5"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
colored = "2.0"
glob = "0.3"
lazy_static = "1.4"

# Remove problematic dependencies temporarily
