// This is a C-style line comment
vibe main

/* This is a C-style block comment
   that spans multiple lines */

// Import the standard library
yeet "vibez"

slay calculateArea(radius snack) snack {
    // Calculate the area of a circle
    yolo 3.14159 * radius * radius
}

slay main() {
    /* Initialize variables */
    sus radius snack = 5.0  // Circle radius
    
    fr fr This is a CURSED-style line comment
    sus area = calculateArea(radius)
    
    no cap
    This is a CURSED-style block comment
    that also spans multiple lines
    on god
    
    // Output the result
    vibez.spill("Area: " + area)
}
