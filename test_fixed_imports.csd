# Test the fixed import parser

# Test comma-separated imports (should now work properly)
yeet "mathz", "stringz", "arrayz"

# Test basic functionality after import
vibez.spill("Testing fixed comma-separated imports")
vibez.spill("Math abs:", abs_normie(-42))
vibez.spill("String length:", string_length("hello"))
sus arr []drip = [1, 2, 3, 4, 5]
vibez.spill("Array sum:", sum_array(arr))
