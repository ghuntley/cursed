fr fr Working stdlib test

slay print_message(msg tea) {
    fr fr For now, we'll print manually
    sus prefix tea = "CURSED: "
}

slay main_character() {
    sus message tea = "Hello from working stdlib!"
    print_message(message)
    
    sus x normie = 42
    sus y normie = 10
    sus sum normie = x + y
    
    fr fr Test completed successfully
}
