yeet "mathz"

slay test_real_mathz() {
    sus a drip = 10.0
    sus b drip = 5.0
    sus result drip = mathz.add_two(a, b)
    vibez.spill("Real mathz test completed")
}

slay main_character() {
    test_real_mathz()
}
