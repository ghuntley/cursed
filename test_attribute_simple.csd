# Simple CURSED program without attributes for baseline testing
slay add(x drip, y drip) drip {
    damn x + y
}

slay main() drip {
    vibez.spill("Testing basic functionality")
    sus result drip = add(5, 3)
    vibez.spill("Result:", result)
    damn 0
}
