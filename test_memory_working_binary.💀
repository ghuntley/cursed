yeet "vibez"
yeet "memory"

slay main_character() {
    sus size drip = 1024
    sus addr drip = memory.malloc(size)
    memory.memset(addr, 0, size)
    memory.free(addr)
    vibez.spill("Memory working in binary!")
}
