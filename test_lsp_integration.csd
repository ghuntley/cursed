// Test CURSED file for LSP integration
// This file tests various LSP features

slay main() {
    sus greeting tea = "Hello, CURSED!"
    vibez.spill(greeting)
    
    sus count normie = 42
    sus flag lit = based
    
    facts flag {
        vibez.spill("Flag is based!")
    }
}

squad Point {
    spill x normie
    spill y normie
}

collab Drawable {
    slay draw()
}
