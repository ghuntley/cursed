vibe main
yeet "vibez"

squad Node {
    data normie  
    next ඞNode
}

slay create_with_ascii(value normie) ඞNode {
    sus node ඞNode = Node{data: value, next: nah}
    damn node
}

slay main_character() {
    vibez.spill("Testing ASCII struct name")
}
