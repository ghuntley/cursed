vibe main

yeet "vibez"

slay test_unicode() {
    sus a normie = 42
    sus ptr_a ඞnormie = ඞa
    damn *ptr_a
}

slay main_character() {
    vibez.spill("Testing Unicode")
    sus result normie = test_unicode()
    vibez.spill(result)
}
