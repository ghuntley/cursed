vibe main

yeet "vibez"

slay test_function(param normie) {
    ready (param % 2 == 0) {
        vibez.spill("Even")
    } otherwise {
        vibez.spill("Odd")  
    }
    vibez.spill("After if")
}

slay main_character() {
    test_function(42)
    damn  
}
