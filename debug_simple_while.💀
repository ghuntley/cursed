vibe main

yeet "vibez"

slay test_simple(n normie) {
    sus i normie = 1
    periodt (i <= n) {
        vibez.spill(i)
        i = i + 1
    }
}

slay main_character() {
    test_simple(3)
    damn
}
