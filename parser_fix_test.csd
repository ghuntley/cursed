vibe main_character
yeet "vibez"

slay test_function(x normie) normie {
    damn x * 2
}

slay main_character() {
    vibez.spill("Testing parser fixes")
    sus result normie = test_function(21)
    vibez.spill(result)
}
