vibe main

yeet "vibez"

slay test_if() {
    sus unknown_var normie = 42
    ready (unknown_var % 3 == 0) {
        vibez.spill("Divisible by 3")
    } otherwise {
        vibez.spill("Not divisible by 3")
    }
}

slay main_character() {
    test_if()
    damn
}
