vibe testpackage;

facts PI = 3.14159;
facts E = 2.71828;

slay calculate_circle_area(radius normie) normie {
    yolo PI * radius * radius;
}

slay main() {
    sus area = calculate_circle_area(5);
    vibez.spill("Circle area calculated");
    vibez.spill(area);
    yolo 0;
}
