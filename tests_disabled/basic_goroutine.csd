vibe main

slay simple() {
    sus i = 0;
    bestie i < 5; i++ {
        print(i);
    }
}

slay main() {
    fr fr Start a goroutine that prints 0 to 4
    stan simple();
    
    fr fr Let the main thread sleep a bit to allow the goroutine to execute
    sleep(1);
}