vibe test_composite;

fr fr Test file for composite types in CURSED

fr fr Array test
slay main() {
    sus numbers normie = crew[1, 2, 3, 4, 5];
    sus first = numbers[0];
    sus last = numbers[4];
    
    lowkey (first == 1 && last == 5) {
        yolo 1;
    } highkey {
        yolo 0;
    }
}