yeet "core"
yeet "vibez"

slay small_function(x normie) normie {
    damn x * 2
}

slay main() {
    bestie i := 0; i < 10; i++ {
        sus result normie = small_function(i)
        vibez.spill(result)
    }
}
