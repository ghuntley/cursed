vibe main
yeet "vibez"

squad Node {
    data normie
    next Node
}

slay main_character() {
    vibez.spill("Before creation")
    sus first Node = Node{data: 1, next: nah}
    sus second Node = Node{data: 2, next: nah}
    vibez.spill("Before member access")
    first.next = second
    vibez.spill("After member access")
}
