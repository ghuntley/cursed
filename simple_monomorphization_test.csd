# Simple test to verify monomorphization system integration

slay main() {
    # Test basic compilation with monomorphization integration
    sus value normie = 42
    vibez.spill("Value:")
    vibez.spill(value)
    
    sus text tea = "Hello from monomorphization"
    vibez.spill(text)
    
    sus flag lit = based
    vibez.spill("Flag:")
    vibez.spill(flag)
}
