yeet "regex"
yeet "vibez"

slay main_character() {
    vibez.spillln("Testing regex module...")
    
    sus result tea = regex.find("hello", "hello world")
    vibez.spillln("Find result:")
    vibez.spillln(result)
}
