vibe main
yeet "vibez"

slay binary_search(arr yikes normie, target normie) normie {
    sus left drip = 0
    sus right drip = arr.len() - 1
    
    periodt left <= right {
        sus mid drip = left + (right - left) / 2
        lowkey arr[mid] == target {
            damn mid
        }
        lowkey arr[mid] < target {
            left = mid + 1
        } highkey {
            right = mid - 1
        }
    }
    
    damn -1
}

slay main_character() {
    vibez.spill("Testing parser")
}
