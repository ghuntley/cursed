yeet "mathz"

# Test the fixed syntax
slay test_fixed_syntax() cringe {
    # Test sqrt_meal logic
    sus guess meal = 5.0
    sus diff meal = 1.0
    sus iterations normie = 0
    
    vibes diff > 0.00001 {
        lowkey iterations >= 100 {
            break
        }
        diff = diff / 2.0
        iterations = iterations + 1
    }
    
    vibez.spill("Syntax test passed")
}

test_fixed_syntax()
