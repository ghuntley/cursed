vibe main
yeet "vibez"

slay main_character() {
    fr Basic integer
    sus normie int_val = 42
    vibez.spill(int_val)
    
    fr Basic float  
    sus meal float_val = 3.14
    vibez.spill(float_val)
    
    fr String variable
    sus txt str_val = "Hello CURSED!"
    vibez.spill(str_val)
    
    fr Boolean variable
    sus lit bool_val = based
    vibez.spill(bool_val)
    
    fr Different integer types
    sus smol small_int = 100
    sus mid medium_int = 1000
    sus thicc large_int = 10000
    
    vibez.spill(small_int)
    vibez.spill(medium_int)
    vibez.spill(large_int)
}
