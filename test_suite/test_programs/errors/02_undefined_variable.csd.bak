// Undefined Variable Test
// Tests: Variable scope and error handling
// Expected: Both modes should detect undefined variables consistently

fn main() {
    yap("=== Undefined Variable Test ===")
    
    yap("Using undefined variable:")
    yap(undefined_var)
    
    yap("This should not execute")
}
