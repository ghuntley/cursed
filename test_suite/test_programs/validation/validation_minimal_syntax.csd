vibe main_character

fr fr Test: Minimal CURSED syntax validation
fr fr Purpose: Test basic parsing without stdlib dependencies
fr fr Expected: Should parse and execute basic operations

slay main_character() {
    fr fr Basic variable declarations
    sus x normie = 42
    sus y normie = 10
    sus z normie = x + y
    
    fr fr Simple control flow
    ready x > y {
        sus result normie = x * 2
    } otherwise {
        sus result normie = y * 2
    }
    
    fr fr Simple arithmetic
    sus sum normie = x + y + z
    sus product normie = x * y
    
    damn 0
}
