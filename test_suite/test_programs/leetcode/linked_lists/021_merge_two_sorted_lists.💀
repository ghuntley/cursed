vibe main

yeet "vibez"

fr fr LeetCode #21: Merge Two Sorted Lists
fr fr Merge two sorted linked lists using ඞ pointers recursively

be_like ListNode squad {
    val normie
    next ඞListNode
}

slay create_node(val normie) ඞListNode {
    sus node ඞListNode = ඞListNode{val: val, next: nah}
    damn node
}

slay merge_two_lists(list1 ඞListNode, list2 ඞListNode) ඞListNode {
    ready (list1 == nah) {
        damn list2
    }
    ready (list2 == nah) {
        damn list1
    }
    
    ready (list1.val <= list2.val) {
        list1.next = merge_two_lists(list1.next, list2)
        damn list1
    } otherwise {
        list2.next = merge_two_lists(list1, list2.next)
        damn list2
    }
}

slay create_list_from_array(arr []normie) ඞListNode {
    ready (len(arr) == 0) {
        damn nah
    }
    
    sus head ඞListNode = create_node(arr[0])
    sus current ඞListNode = head
    
    bestie i := 1; i < len(arr); i++ {
        current.next = create_node(arr[i])
        current = current.next
    }
    
    damn head
}

slay print_list(head ඞListNode) {
    sus current ඞListNode = head
    periodt (current != nah) {
        vibez.spill(current.val)
        current = current.next
    }
}

slay test_merge_two_lists() {
    vibez.spill("=== LeetCode #21: Merge Two Sorted Lists (ඞ) ===")
    
    fr fr Test case 1: [1,2,4] + [1,3,4] = [1,1,2,3,4,4]
    sus list1 ඞListNode = create_list_from_array([]normie{1, 2, 4})
    sus list2 ඞListNode = create_list_from_array([]normie{1, 3, 4})
    sus merged1 ඞListNode = merge_two_lists(list1, list2)
    
    vibez.spill("Test 1 - Expected: [1,1,2,3,4,4]")
    print_list(merged1)
    
    fr fr Test case 2: [] + [] = []
    sus empty1 ඞListNode = nah
    sus empty2 ඞListNode = nah
    sus merged2 ඞListNode = merge_two_lists(empty1, empty2)
    
    vibez.spill("Test 2 - Expected: []")
    ready (merged2 == nah) {
        vibez.spill("Empty list (null)")
    }
    
    fr fr Test case 3: [] + [0] = [0]
    sus empty ඞListNode = nah
    sus single ඞListNode = create_list_from_array([]normie{0})
    sus merged3 ඞListNode = merge_two_lists(empty, single)
    
    vibez.spill("Test 3 - Expected: [0]")
    print_list(merged3)
    
    vibez.spill("=== Merge Lists Complete (Sus Pointers Rock!) ===")
}

slay main_character() {
    test_merge_two_lists()
}
