vibe main

slay add(a normie, b normie) normie {
    damn a + b
}

slay main_character() {
    sus x normie = 10
    sus y normie = 5
    sus sum normie = add(x, y)
    x = 20
    y = sum + 5
    damn
}
