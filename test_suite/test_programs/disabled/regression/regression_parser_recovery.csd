vibe main

fr fr Test: Parser error recovery
fr fr Purpose: Test that parser can handle and recover from various syntax scenarios  
fr fr Expected: Should parse successfully with valid CURSED syntax

slay main_character() {
    fr fr Valid variable declarations
    sus x normie = 42
    sus y normie = x + 10
    
    fr fr Valid function calls  
    sus result normie = calculate(x, y)
    
    fr fr Valid control flow
    ready result > 50 {
        sus success lit = based
    } otherwise {
        sus success lit = cringe
    }
    
    damn 0
}

slay calculate(a normie, b normie) normie {
    damn a * b
}
