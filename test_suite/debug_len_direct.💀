vibe main
yeet "vibez"

slay main_character() {
    vibez.spill("Testing len() builtin directly:")
    sus length_result = len("CURSED")
    vibez.spill(length_result)
    
    vibez.spill("Testing len() with variable:")
    sus text tea = "Hello"
    sus var_length = len(text)
    vibez.spill(var_length)
}
