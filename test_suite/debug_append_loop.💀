vibe main
yeet "vibez"

slay main_character() {
    vibez.spill("Debug append in loop")
    
    sus test_array normie[value] = normie[value]{}
    vibez.spill("Start length:", len(test_array))
    
    bestie i := 0; i < 3; i++ {
        vibez.spill("Loop iteration", i)
        test_array = append(test_array, i)
        vibez.spill("After append, length:", len(test_array))
    }
    
    vibez.spill("Final length:", len(test_array))
}
