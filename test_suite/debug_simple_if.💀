vibe main
yeet "vibez"

slay test_return(x normie) normie {
    ready x > 0 {
        damn 1
    }
    damn 0
}

slay main_character() {
    vibez.spill("Testing simple if function")
    sus result normie = test_return(5)
    vibez.spill(result)
}
