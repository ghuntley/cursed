vibe main
yeet "vibez"

fr fr Test module collision detection (duplicate)
slay test_module_collision() {
    vibez.spill("Testing module collision detection - DUPLICATE VERSION")
    vibez.spill("This module should trigger a collision if loaded after the first one")
}

fr fr Different function for testing collision
slay collision_test_function_different() normie {
    damn 999
}

slay main_character() {
    test_module_collision()
    sus result normie = collision_test_function_different()
    vibez.spill("Result: " + tea(result))
}
