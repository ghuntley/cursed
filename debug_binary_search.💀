vibe main
yeet "vibez"

slay search(nums normie[], target normie) normie {
    vibez.spill("Entering search function")
    sus left normie = 0
    vibez.spill("Set left = 0")
    
    vibez.spill("About to call len(nums)")
    sus right normie = len(nums) - 1
    vibez.spill("Set right =", right)

    bestie (left <= right) {
        vibez.spill("In while loop, left:", left, "right:", right)
        sus mid normie = left + (right - left) / 2
        vibez.spill("mid =", mid)
        
        vibez.spill("About to access nums[mid]")
        ready (nums[mid] == target) {
            damn mid
        } nah ready (nums[mid] < target) {
            left = mid + 1
        } nah {
            right = mid - 1
        }
    }

    damn -1
}

slay main_character() {
    vibez.spill("Starting test")
    sus nums normie[] = [1, 3, 5, 6, 7, 8, 10]
    vibez.spill("Created array")
    sus result normie = search(nums, 6)
    vibez.spill("Result:", result)
}
