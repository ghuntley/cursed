yeet "vibez"
yeet "json"

slay main_character() {
    sus json_text drip = "{\"test\":true}"
    sus parsed drip = json.parse(json_text)
    sus stringified drip = json.stringify("data")
    sus is_valid drip = json.validate(json_text)
    vibez.spill("JSON working in binary!")
}
