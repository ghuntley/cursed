# Debug bootstrap compilation issue
slay main() normie {
    vibez.spill("CURSED Stage 2 Compiler - Self-Hosting Edition")
    
    # Test simple boolean assignment
    sus test_result lit = based
    lowkey (test_result) {
        vibez.spill("Bootstrap compilation successful!")
        damn 0
    } highkey {
        vibez.spill("Bootstrap compilation failed!")
        damn 1
    }
}
