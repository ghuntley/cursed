vibe main
yeet "collections"
yeet "vibez"

slay main_character() {
    vibez.spill("About to call collections.length...")
    
    fr fr Test with explicit array creation
    sus test_arr normie[value] = {1, 2, 3}
    
    fr fr Direct function call - this should either work or give UndefinedFunction
    collections.length(test_arr)
    
    vibez.spill("If you see this, the call succeeded")
}
