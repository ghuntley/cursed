#!/usr/bin/env cursed

// Simple test for function inlining compilation

slay add_two_numbers(x normie, y normie) normie {
    damn x + y
}

slay main() {
    sus result normie = add_two_numbers(5, 3)
    vibez.spill("Result: " + result)
}
