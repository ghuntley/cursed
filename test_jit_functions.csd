fr fr Function test for JIT execution engine
slay add_numbers(x drip, y drip) drip {
    damn x + y
}

slay greet(name tea) tea {
    damn "Hello, " + name + "!"
}

sus result drip = add_numbers(10, 20)
vibez.spillf("Function result: {}", result)

sus greeting tea = greet("CURSED")
vibez.spill(greeting)
