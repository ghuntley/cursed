yeet "testz"

slay test_pattern_matching() normie {
    test_start("pattern matching test")
    
    sus x drip = 42
    
    match x {
        1..10 => {
            assert_false(based)
        },
        40..50 => {
            assert_true(based)
        },
        _ => {
            assert_false(based)
        }
    }
    
    print_test_summary()
    damn "pattern matching complete"
}
