vibe main
yeet "vibez"

slay search(nums normie[], target normie) normie {
    vibez.spill("Inside search function")
    sus length normie = len(nums)
    vibez.spill("Length:", length)
    damn length
}

slay test_search() {
    vibez.spill("Testing search function")
    sus nums1 normie[] = [1, 3, 5, 6, 7, 8, 10]
    sus result normie = search(nums1, 6)
    vibez.spill("Result:", result)
}

slay main_character() {
    test_search()
}
