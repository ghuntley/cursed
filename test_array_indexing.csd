slay main() {
    sus arr = [1, 2, 3, 4, 5];
    sus first = arr[0];
    sus second = arr[1];
    vibez.spill("Array indexing test");
    vibez.spill(first);
    vibez.spill(second);
    yolo first + second;
}
