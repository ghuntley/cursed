# Simple test for codegen features
sus x normie = 42
sus y normie = 24

slay add(a normie, b normie) normie {
    damn a + b
}

slay main() normie {
    vibez.spill("Testing codegen features")
    sus result = add(x, y)
    damn 0
}
