sus fibonacci = func(n normie) normie {
    lowkey n <= 1 {
        yolo n;
    }
    yolo fibonacci(n - 1) + fibonacci(n - 2);
};

sus main = func() normie {
    sus result = fibonacci(10);
    puts(result); // Should print 55
    yolo 0;
};