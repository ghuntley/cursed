vibe main
yeet "vibez"
yeet "mathz"

slay main_character() {
    vibez.spill("🔥 Testing Extended LLVM Pipeline!")
    
    // Test basic variables
    sus x drip = 42
    sus y drip = 24
    
    // Test binary operations 
    sus result drip = x + y
    vibez.spill("Addition result:")
    vibez.spill(result)
    
    sus diff drip = x - y
    vibez.spill("Subtraction result:")
    vibez.spill(diff)
    
    // Test method calls with variables
    vibez.spill("x value:")
    vibez.spill(x)
    
    vibez.spill("y value:")
    vibez.spill(y)
    
    vibez.spill("✅ Extended LLVM pipeline test complete!")
    rn 0
}
