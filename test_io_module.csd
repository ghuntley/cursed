yeet "io"

slay main() {
    vibez.spill("Testing IO module...")
    
    sus content tea = "Hello, world!"
    sus result lit = io.write_file("test_io.txt", content)
    vibez.spill("Write result: " + tea(result))
    
    vibez.spill("Test complete!")
}

main()
