# Test select statement with mood and basic keywords
yeet "vibez"

slay main() {
    vibez.spill("Testing select statement with mood and basic keywords...")
    
    # Test basic ready/mood/basic syntax parsing
    ready {
        basic:
            vibez.spill("✅ Basic select with mood/basic parsing works")
    }
    
    vibez.spill("✅ Select statement parsed successfully")
}

main()
