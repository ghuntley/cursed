then study specs/* to understand the application and use a subagent to resolve the failing tests. make sure you run cargo test for that test to ensure you have fixed it. use up to 10 subagents if required but only one subagent when doing a test/build. if the tests pass, do a git commit and push it.  you may resolve nix (devenv.nix) and rust infrastructure/configuration concerns if it stops you from proceeding.
