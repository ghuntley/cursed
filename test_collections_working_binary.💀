yeet "vibez"
yeet "collections"

slay main_character() {
    sus vec drip = collections.Vec_new()
    vec = collections.Vec_push(vec, 42)
    sus len drip = collections.Vec_len(vec)
    vibez.spill("Collections working in binary!")
}
