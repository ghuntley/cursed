vibe main
yeet "vibez"  
yeet "collections"

slay main_character() {
    vibez.spill("About to call collections function...")
    
    fr fr Try a direct call without variable assignment first
    sus test_arr normie[value] = {1, 2, 3}
    sus result normie = collections.length(test_arr)
    
    vibez.spill("Function call completed, result:")
    vibez.spill(result)
}
