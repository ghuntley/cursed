# Simple Select Statement Test

yeet "testz"

slay test_simple_ready() {
    test_start("Simple Ready Statement")
    
    sus executed lit = cringe
    
    ready {
        basic: {
            executed = based
            vibez.spill("Default case executed")
        }
    }
    
    assert_true(executed)
    print_test_summary()
}

slay main() {
    test_simple_ready()
}
