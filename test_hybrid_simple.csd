fr fr Test proven working CURSED and fallback Zig

sus literal_value drip = explicit_return.get_number()
sus param_value drip = param_test.echo_param(50)

fr fr This should fallback to Zig stringz since CURSED stringz is complex
sus str_length drip = stringz.length("test")
