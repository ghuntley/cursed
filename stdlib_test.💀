yeet "vibez"

slay main_character() {
    vibez.spill("=== CURSED Standard Library Test ===")
    
    # Basic arithmetic
    sus a normie = 10
    sus b normie = 5
    sus sum normie = a + b
    sus diff normie = a - b
    sus prod normie = a * b
    
    vibez.spill("Arithmetic test:")
    vibez.spill("10 + 5 =", sum)
    vibez.spill("10 - 5 =", diff) 
    vibez.spill("10 * 5 =", prod)
    
    # String operations
    sus greeting tea = "Hello"
    sus name tea = "CURSED"
    
    vibez.spill("String test:")
    vibez.spill(greeting, name)
    
    # Boolean logic
    sus truth lit = based
    sus lie lit = cringe
    
    vibez.spill("Boolean test:")
    vibez.spill("Truth:", truth)
    vibez.spill("Lie:", lie)
    
    vibez.spill("=== All tests completed ===")
}
