# Test if select statements parse correctly
yeet "vibez"

slay main() {
    vibez.spill("Testing select statement parsing...")
    
    # Test basic ready/basic syntax parsing
    ready {
        basic:
            vibez.spill("✅ Basic select parsing works")
    }
    
    vibez.spill("✅ Select statement parsed successfully")
}

main()
