use crate::error::CursedError;
/// Token types for the lexer
#[derive(Debug, Clone, PartialEq)]
pub struct Token {
#[derive(Debug, Clone, PartialEq)]
pub enum TokenType {
    // Basic tokens
    // Keywords
    // Operators
    // Delimiters
    // Special
}
