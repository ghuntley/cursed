// Comprehensive CURSED test program
slay main() {
    vibez.spill("=== CURSED Comprehensive Test ===");
    
    // Basic arithmetic and variables
    sus x normie = 10;
    sus y normie = 20;
    sus result normie = x + y;
    vibez.spill("Basic arithmetic: 10 + 20 = ");
    vibez.spill(result);
    
    // Function calls
    sus sum normie = add(x, y);
    vibez.spill("Function call result: ");
    vibez.spill(sum);
    
    // Conditional logic
    lowkey result > 25 {
        vibez.spill("Result is greater than 25");
    } highkey {
        vibez.spill("Result is 25 or less");
    }
    
    // String operations
    sus message tea = "Hello, CURSED World!";
    vibez.spill(message);
    
    // Boolean logic
    sus is_greater truth = result > 25;
    lowkey is_greater {
        vibez.spill("Boolean logic works!");
    }
    
    yolo 0;
}

slay add(a normie, b normie) normie {
    yolo a + b;
}
