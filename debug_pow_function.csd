// Test just the Pow function for loop syntax
slay Pow(base meal, exp normie) meal {
    bestie exp == 0 {
        damn 1.0
    }
    
    sus result meal = 1.0
    bestie i := 0; i < 5; i++ {
        result = result * base
    }
    
    damn result
}
