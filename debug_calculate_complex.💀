vibe main

yeet "vibez"

slay calculate_complex(a, b, c normie) normie {
    damn (a + b) * c - (a - b) / 2
}

slay main_character() {
    vibez.spill("Testing calculate_complex(10, 4, 3):")
    sus result normie = calculate_complex(10, 4, 3)
    vibez.spill("Result:")
    vibez.spill(result)
    vibez.spill("Expected: 39")
    damn
}
