// Test LLVM optimization passes implementation
slay test_optimization_function(x normie) normie {
    sus result normie = x * 2
    sus doubled normie = result + result
    sus tripled normie = doubled + result
    damn tripled
}

slay main() {
    sus input normie = 42
    sus output normie = test_optimization_function(input)
    vibez.spill("Optimization test result: ", output)
}
