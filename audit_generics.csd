// Test generics
slay generic_function<T>(x T) T {
    damn x
}

// Test interfaces
collab Drawable {
    slay draw() tea
}

squad Circle {
    spill radius normie
}

slay Circle.draw() tea {
    damn "Drawing circle"
}

// Test pattern matching
slay test_match(x normie) tea {
    swayze (x) {
        1 => "one"
        2 => "two"  
        _ => "other"
    }
}

sus result tea = test_match(1)
vibez.spill(result)
