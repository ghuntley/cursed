yeet "mathz"

slay main_character() {
    yap("=== CURSED Basic Math Test ===")
    
    // Test 1: Basic mathz.add_two()
    sus result1 normie = mathz.add_two(5, 3)
    yap("mathz.add_two(5, 3) =")
    yap(result1)
    
    // Test 2: Zero handling
    sus result2 normie = mathz.add_two(0, 7)
    yap("mathz.add_two(0, 7) =")
    yap(result2)
    
    // Test 3: Negative numbers
    sus result3 normie = mathz.add_two(-2, 8)
    yap("mathz.add_two(-2, 8) =")
    yap(result3)
    
    yap("=== Test Complete ===")
}
